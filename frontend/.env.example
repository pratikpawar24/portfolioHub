# Base URL of the PortfolioHub Spring Boot API (no trailing slash).
# See src/lib/api/config.ts — nothing else in this app should hardcode
# a backend host.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
